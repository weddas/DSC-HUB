# ==================================================================
#  DSC-HeatMat — device stub  (v2.4)
#  Sonoff BASIC R2 v1.4 · ESP8285 · GPIO12 relay / GPIO0 button / GPIO13 LED
#  ------------------------------------------------------------------
#  ⚠ NEVER put logic in a stub — it belongs in dsc-sonoff-common.yaml.
#  ⚠ `name:` is the entity_id contract: switch.dsc_heatmat_main_relay
#     is referenced by the demand_follower automation + dashboards.
#
#  API-loss grace: 90s
#  # Under-pot grow mat (2x4 clone tent). Low power, slow thermal mass —
# 90s grace rides out a routine HA restart without dropping root heat.
# ==================================================================

substitutions:
  name: dsc-heatmat
  friendly_name: DSC-HeatMat
  ap_ssid: "DSC-HeatMat Fallback Hotspot"
  api_key_val: !secret dsc_heatmat_api_key
  ota_password_val: !secret dsc_heatmat_ota_password
  ap_password_val: !secret dsc_heatmat_ap_password
  api_grace_ms: "90000"

packages:
  common: !include dsc-sonoff-common.yaml
