# ==================================================================
#  DSC-POT#1 — device stub  (v2.4)
#  ESP32 · RS485 7-in-1 NPK soil probe
#  ------------------------------------------------------------------
#  ⚠ NEVER put logic in a stub — it belongs in dsc-pot-common.yaml.
#  ⚠ `name:` is the entity_id contract: sensor.dsc_pot1_soil_*
#     (7 sensors) are referenced by the four HA packages, the DSC Grow
#     dashboard, and the hub's closed-loop grow mat.
# ==================================================================

substitutions:
  name: dsc-pot1
  friendly_name: DSC-POT#1
  api_key_val: !secret dsc_pot1_api_key
  ota_password_val: !secret dsc_pot1_ota_password
  ap_password_val: !secret dsc_pot1_ap_password

packages:
  common: !include dsc-pot-common.yaml
