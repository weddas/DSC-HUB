# ==================================================================
#  DSC pot · kit WiFi (join DSC-Setup-* SoftAP for provisioning)
# ==================================================================
wifi:
  power_save_mode: none
  # Match hub: allow scan onto learned preferred BSSID after bounce.
  fast_connect: false
  ap:
    ssid: "${friendly_name} Fallback"
    password: ${ap_password_val}
