# DSC-HUB panel frontend — typecheck and SPA build on every push/PR touching frontend.
name: Frontend CI

on:
  push:
    branches: [master, main]
    paths:
      - "homeassistant/custom_components/dsc_hub/frontend/**"
      - ".github/workflows/frontend-ci.yml"
  pull_request:
    paths:
      - "homeassistant/custom_components/dsc_hub/frontend/**"
      - ".github/workflows/frontend-ci.yml"

defaults:
  run:
    working-directory: homeassistant/custom_components/dsc_hub/frontend

jobs:
  build:
    name: tsc + SPA build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: homeassistant/custom_components/dsc_hub/frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npx tsc --noEmit

      - name: Build SPA
        run: npm run build:spa
