# DSC-HUB Phase 1 audit — 2026-08-17

Repo: `https://github.com/weddas/DSC-HUB` · probed HEAD `6092459` (`chore(hacs): sync dist/ from homeassistant/www`) on `master` matching `origin/master`.
Live HA: `http://192.168.86.3:8123` · CannaLib: `https://cannalib.plausible-deniability.net` · Notion Bug Box: `collection://f18ec9d2-1ed1-4033-96b8-726971429250`.

This is an audit, not a completion pass. No Phase 2 work starts until the user accepts a plan.

Probe window: 2026-08-17 ~18:05–18:25 AEST. Hub was **online** at first Mission paint, then **flapped off** mid-session (`binary_sensor.dsc_hub_link` `on` → `off` at `2026-08-17T08:17:51Z`). Numbers below are observed states, not invented chemistry.

Screenshots (Cursor browser, this session):

- Mission (hub up): `c:\Users\cmgwe\AppData\Local\Temp\cursor\screenshots\audit-ha-dsc-hub-2026-08-17.png`
- Twin click / HELD: auto-captured on `#/live/twin` while link dropped
- Lovelace fallback: `#/dsc-hub-pro/ops` (Fleet map IIFE + config-error banner)
- Climate HELD: `#/live/climate` after flap

---

## 1. Inventory

### Firmware (`firmware/v4/`)

| Stub | Body / project.version in tree | Role |
|---|---|---|
| `dsc-hub.yaml` / `dsc-hub-kit.yaml` | `digital_emotions.dsc-hub` **5.2.0** | Climate ladder, Full Auto, light ledger, in-service gates |
| `dsc-control.yaml` / kit | `digital_emotions.dsc-control` **6.0.0.0** | CYD panel |
| `dsc-pot1.yaml`–`dsc-pot4.yaml` / kits | `digital_emotions.dsc-pot` **5.2.0** | RS485 soil nodes |
| `dsc-bridge.yaml` / kit | `digital_emotions.dsc-bridge` **5.2.0** | WT32-ETH01 SoftAP Anchor |
| `dsc-heater.yaml`, `dsc-heatmat.yaml`, `dsc-humidifier.yaml`, `dsc-de-humidifier.yaml` | `digital_emotions.dsc-sonoff` **5.2.0** | Relay followers (no ESP-NOW) |
| `dsc-appliance-bridge.yaml` | pointer only | Production is `dsc-bridge.yaml` |

ESP-NOW packages stay in tree (`dsc-hub-espnow-primary.yaml`) and are still included by the hub stub. Product policy (`d92d306`, FOLLOWUPS 2026-08-16): **parked**, not the next campaign.

### HA packages (`homeassistant/packages/dsc_v4_*.yaml`)

Core, climate physics/stats/learn, sensor trust/cal, pots (alerts/coherence/stats/correlation), root zone, vessel, tank + tank dummies, strain/nutrient/light/medium catalogs, CannaLib API, automations, notify, fleet heal, version/fleet chip, build-plant, pot-tent, actuator efficacy, device cal, alert count, bridge.

### Custom panel

`homeassistant/custom_components/dsc_hub/` — React `/dsc-hub` (`PANEL_URL_PATH`). Built bundle `www/dsc-hub-panel.js`.

Nav that exists: Live (Mission / Twin / Climate / 4×8 / 2×4 / Root / Light), Grow (Compose / Research / Roster), Tune (Learning / Analytics), Fleet.

### Lovelace fallback

YAML dashboard `dsc-hub-pro` + `dsc-build-plant`, both `show_in_sidebar: false` (`homeassistant/configuration.snippet.yaml`). Cards in `homeassistant/www/` and HACS `dist/`.

### Sync

`scripts/ha-sync.sh` (needs `HA_HOST` + `HA_TOKEN`; never syncs `secrets.yaml`). Add-on path: `scripts/ADDON.md`. This host: `HA_TOKEN` unset.

### CannaLib

Live API 0.3.5-stdlib. Hub `services/cannalib/docker-compose.yml` is marked **STALE** (Recreate from the CannaLib repo, not Hub).

### Operator docs

`INSTALL.md`, `RELEASE.md` (still tagged **v5.1.0**), `docs/FOLLOWUPS.md`, `docs/ASSIST-MCP.md`, `docs/HA-SCAFFOLD.md`, `docs/DSC-BRAIN.md`.

---

## 2. Surface matrix

| Area | Result | Evidence |
|---|---|---|
| **Hub firmware YAML** | **PASS** (code) / **BLOCKED** (compile) | Body `project.version` 5.2.0. Full Auto default ON + boot force unless Takeover (`dsc-hub-v4_0.yaml`). Emergency >35 °C fans-only if AC OOS (`2984–2986`). Light ledger entities present. `post_connect_roaming: false` (`dsc-hub-wifi-lab.yaml`). **Compile:** `esphome` not on PATH; N-008 UNC compile still the Windows rule. Local copy exists at `C:\Users\cmgwe\esphome-dsc\v4\dsc-hub.yaml` — not compiled this session. |
| **Hub live** | **FAIL** (flap) / **PASS** (honesty gates while up) | First paint: `binary_sensor.dsc_hub_link` **on**, Full Auto **on**, FW **6.0.0.0**, uptime ~1201 s, RSSI −63 dBm, emergency failsafe **off**, AC demand **off**, clone-mister demand **off**. Then link **off** at 08:17:51Z. `sensor.dsc_ha_link_flap_count_24h` = **327**. Do not reopen ESP-NOW. |
| **Control / pots** | **PASS** (gates) / **FAIL** (panel dark + POT3/4 hardware) | In-service live: AC **off**, mister **off**, POT1/2/4 **on**, POT3 **off**. POT3 soil **unavailable**; POT3 chemistry alerts **off**. POT4 moisture **unknown** + Mission **HELD**. Control FW + heap **unavailable** (F-007 unmeasurable). Live pot/hub/sonoff FW **6.0.0.0** vs repo hub/pot body **5.2.0** vs `input_text.dsc_expected_release` **5.2.0** → fleet chip **warn**. |
| **React panel `/dsc-hub`** | **PASS** (nav) / **FAIL** (version + Research copy) / **BLOCKED** (type/select soak) | All primary/secondary tabs present. Mission, Twin URL, Climate, Research loaded. `#/live/twin` changed URL; Twin canvas not isolated in the screenshot (Mission chrome + flap). Type/select-while-ticks **not proven** — Research searchbox click failed (likely TwinKeepAlive canvas intercept). Tab-away rAF **not proven** live. |
| **Twin honesty** | **PASS** (code) / **BLOCKED** (live canvas) | Code: HELD freezes wisps; OOS pads dashed; `entities.main_light` empty; 4×8 shafts Window proxy. Live: after flap, Mission/Climate showed **HELD** / **HUB LINK DOWN** and “not last-good animation.” Canvas-only Twin (no IIFE HUD) not screenshot-proven this pass. |
| **Kit Pulse / Mission** | **PASS** (planned OOS) / **FAIL** (tank alerts) | Planned OOS pills: **AC OOS**, **Clone mister OOS**, **Pot 3 OOS**, **Tank OOS**. `binary_sensor.dsc_reduced_kit` **off**. No REDUCED KIT banner. **Humidifier missing** (FW unavailable) is labeled, not reduced-kit. Alerts = tank EC/pH only — see Lies. |
| **Climate / Light / Root** | **PASS** (VPD entity + HELD chips) / **FAIL** (Got 0.0 while HELD) | `sensor.dsc_hub_room_vpd_kpa` **exists** (1.05 kPa while hub up). Fallback `sensor.dsc_hub_room_vpd` **absent**. Climate after flap: Room T/RH/VPD chips **— HELD**. Want/Got bars painted **Got 0.0 °C / 0 %** — unavailable plotted as zero. `sensor.dsc_cfm_intake_*_allocated` **null**. Exhaust allocated **15.3 CFM** while up. Window `binary_sensor.dsc_hub_4x8_window_open` **unavailable** after flap. GPIO lamp empty. |
| **Compose / Research** | **FAIL** (honesty) | Research subtitle still: “Catalog browser over `/local/dsc-catalog` indexes.” Empty browse listed merch/rosin/capsule SKUs (`##Wonka Bar- Thin Mint (300mg)`, `#1003 1500mg Raw Capsule`, `"O" Lubricant`). Typeahead vs live CannaLib **not proven in the panel** (click blocked). DecisionLayer not exercised. |
| **Lovelace fallback** | **PASS** (hidden + renders) / **FAIL** (config error + dist drift) | Sidebar has **DSC-HUB** only — no DSC-HUB Pro. `/dsc-hub-pro/ops` loads YAML Ops + Fleet map IIFE. Banner: **Configuration error**. Live chip **FLEET DRIFT** / **HUB OFFLINE** during flap. |
| **HA cache / versions** | **FAIL** | Live `sensor.dsc_ha_surface_version` = **7.1.3**. Panel chrome paints **SURFACE 7.1.4**. Bundle const `7.1.8-cutover`. `const.py` `SURFACE_VERSION` = `7.1.4`. `ensureLocalCards.ts` still `7.1.7-bar-raise` in source. |
| **CannaLib** | **PASS** (API) / **FAIL** (GSC alias + merch) | `GET /health` → `{"status":"ok","version":"0.3.5-stdlib","auth":"public"}`. `GET /v1/catalogs/strains?q=og%20kush` → 200, first hit `OG Kush` `matched_via: name`, others `fts`. `q=gsc` → first hit **GSC / Kannabia**, not Girl Scout Cookies via `science_alias`. |
| **Sync** | **PASS** (script) / **FAIL** (stale compose secret) | `ha-sync.sh` still the documented path. `HA_TOKEN` unset here so sync not run. Stale Hub `services/cannalib/docker-compose.yml` still embeds `CANNALIB_API_KEY`. |
| **Stashes** | **PASS** (inventoried, not applied) | Six stashes. See §6. |
| **Assist / MCP (I-25)** | **PASS** | HA Assist live context has **no** `sensor.dsc_*` / kit gates. Only DSC-BRIDGE mirror T/RH. Matches `docs/ASSIST-MCP.md` (opt-in, do not silently expose). |

---

## 3. Follow-up table

Shape: **keep** = still true, leave gated. **close-with-evidence** = software can finish if a probe/compile/screenshot proves it. **redesign** = current shape is wrong. **hardware-blocked** = do not fake.

| ID / leftover | Disposition | Foolproof close-out |
|---|---|---|
| **F-001 AC hardware** | **hardware-blocked** | Keep `input_boolean.dsc_ac_in_service` OFF. Live: in-service **off**, AC demand **off**, Climate Cool labeled **AC ○**. Prove emergency fans-only only when tent >35 °C (not seen this session; failsafe **off**). No software “install”. |
| **F-002 clone mister** | **hardware-blocked** | In-service **off**, demand **off**, Kit Pulse **Clone mister OOS**. Demand must stay off. |
| **F-003 POT3 probe** | **hardware-blocked** | In-service **off**, soil **unavailable**, chemistry alerts **off**, mat vote excluded in hub YAML. 2026-08-15 USB: slave `1` silent. Close only after POT2-probe swap. |
| **F-004 SoftAP / BSSID** | **keep** (out of scope nest lock) | SoftAP-primary is the heal path. Do not start a new BSSID campaign. Live fleet already on SoftAP train **6.0.0.0**. |
| **F-005 multi-lever learn** | **keep** (interim) | `dsc_v4_climate_learn.yaml`: when ≥2 air levers ON, Phase A attributes by residual subtract. Phase B wait-only until that soaks. Complete engine needs isolated single-lever samples + residual math that survives flap. Evidence only. |
| **F-006 HA-link flap** | **keep** (measured) | **327** flaps in 24h (`sensor.dsc_ha_link_flap_count_24h`). Link dropped during this audit. Soften/handshake only. **Do not reopen ESP-NOW.** |
| **F-007 panel OOM** | **blocked** (unmeasurable) | Control FW + `sensor.dsc_control_panel_free_heap` **unavailable**. Cannot compare to 5.1.14 heap note until the CYD is on HA. |
| **F-008 SCD41** | **keep** | ADC CO2 stays informational. `sensor.dsc_hub_dynamic_co2_ppm` exists; no real SCD41. |
| **F-009 Full Auto reduced kit** | **PASS** (planned) / leftover (humidifier) | Planned OOS dashed; reduced_kit **off**. UX names missing humidifier. Full Auto was **on** while kit incomplete — allowed if capacity is not faked. |
| **GPIO 4×8 lamp** | **hardware-blocked** | Keep `entities.main_light` empty. Shafts stay dashed Window proxy. Bind 1:1 when GPIO exists. |
| **DSC-Tank firmware** | **hardware-blocked** + **redesign alerts** | Dummies in `dsc_v4_tank_dummies.yaml` (`dsc_tank_in_service` **off**). Tester EC/pH in `dsc_v4_tank.yaml` are **not** gated on in-service → live alerts. Map ids 1:1 when firmware exists; until then gate alerts. |
| **R3F Twin extract** | **keep** (parked) | Do not rewrite a lying scene. Neon APIs must soak first. |
| **N-087 FTS** | **FAIL** (alias) / API FTS **PASS** | CannaLib FTS returns `matched_via: fts`. Hub cards: GSC → Girl Scout Cookies **not** proven. Research still advertises local indexes. |
| **N-012–N-016** | **keep** (deferred) | Smallest honest slices: N-012 no pumps; N-013 dryback track-only; N-014 AC/heater Temp OOS latch (same pattern as hum); N-015 do not claim a complete learn engine; N-016 lab wet cal still open. |
| **Cookie jar in history** | **keep** (optional rewrite) | Removed from tree in `fa244b3`. Still in history (`fc8c4cc`). BFG/`filter-repo` only if the user wants a history rewrite. |
| **I-25 Assist/MCP** | **PASS** | Opt-in only. Live Assist list has no DSC operator entities. |
| **Intake `*_allocated` CFM** | **keep** | Sensors **absent**. Trust line correctly said mixed/nameplate while hub up. Do not invent allocated CFM. Learning write path is the close. |
| **N-008 UNC compile** | **keep** | Still blocked from this NAS workspace. Compile from `C:\Users\cmgwe\esphome-dsc\v4`. |
| **POT4 Modbus / HELD** | **hardware-blocked** | Live: moisture **unknown**, Mission **Got M —\*** **HELD**. Same isolate order as 2026-08-15 (power/AP, then swap POT2 probe). |
| **Peer divergence on OOS POT3** | **redesign** | Mission still tags POT3 **peer divergence** while OOS. Gate that chip on in-service. |
| **dist/ vs www/** | **FAIL** | Card IIFEs match **except** `DSC-HUB.js` and `dsc-system-map-card.js`. Dist copies are **identical 1 068 741 B** (same SHA `32F38CE4039E`); www map is 10 644 B, www `DSC-HUB.js` is 1 036 684 B. HACS dist is lying. |
| **Notion Bug Box** | **open** | 9 In Progress (all **Ok For Agent To Fix**, none Fix Approved), 43 Triaged, 2 Won't Fix, **0 Fixed**. See §5. |

---

## 4. Lies found

UI or packages that claim a sensor, actuator, or catalog row they do not honestly have.

1. **Version chrome** — three strings at once: HA entity **7.1.3**, painted **7.1.4**, bundle **7.1.8-cutover**. Fleet expected **5.2.0** while flashed hub/pots/heater/heatmat/dehum report **6.0.0.0**.
2. **Tank EC/pH alerts on an OOS dummy tank** — `binary_sensor.dsc_tank_ec_out_of_range` / `_ph_out_of_range` **on**; `input_boolean.dsc_tank_in_service` **off**. Templates in `dsc_v4_tank.yaml` have no in-service gate; `dsc_v4_alert_count.yaml` still counts them. Mission’s only two alerts were these.
3. **Got bars = 0 while HELD** — Climate `#/live/climate` after flap: chips said **— HELD**, bars said **Got 0.0 °C / 0 %**. Unavailable must not become zero.
4. **Research merch as cultivar** — empty browse includes capsules, rosin, lubricant, numbered flower lots. Prompt: no merch SKU treated as a cultivar.
5. **Research copy vs live API** — page still says `/local/dsc-catalog` while `catalog.ts` prefers CannaLib. Operator is told the wrong source.
6. **GSC ≠ Girl Scout Cookies** — live `q=gsc` ranks Kannabia **GSC** first; `science_alias` expand not proven on Hub cards.
7. **CannaLib height junk (API, not Hub sensors)** — `q=og kush` returned `Alien Candy Kush Fast` `height_cm: [1500, 2000]` (mm-as-cm). Do not paint that as a grow height.
8. **HACS `dist/`** — `dist/DSC-HUB.js` and `dist/dsc-system-map-card.js` are the same fat file; www counterparts differ. A HACS install from `dist/` is not the www IIFE set.
9. **Stale Hub CannaLib compose** — `services/cannalib/docker-compose.yml` is labeled STALE and still contains a live `CANNALIB_API_KEY`. File says Recreate from the CannaLib repo.
10. **Firmware README / headers** — panel README still says 4.0.11 vs project 6.0.0.0; pot header mentions 4.0 vs 5.2.0; hub changelog still cites tag v5.1.0.
11. **`RELEASE.md` / `INSTALL.md`** — still describe tagged **v5.1.0** and sidebar **DSC-HUB Pro**. Product sidebar is `/dsc-hub`.
12. **POT3 peer-divergence chip** — OOS seat still scored as peer divergence.

Not lies (holding):

- Planned OOS dashed, reduced_kit off.
- AC / mister demands off.
- POT3 chemistry alerts off.
- Room VPD kPa entity exists (while hub up).
- CFM trust line named nameplate/mixed; intake allocated sensors absent.
- Assist MCP does not dump the house DSC set.
- Lovelace `/dsc-hub-pro` hidden from sidebar and still loads.

---

## 5. Notion Bug Box

Connected. Status counts: **In Progress 9**, **Triaged 43**, **Won't Fix 2**, **Fixed 0**. None of the open rows have **Fix Approved**.

### In Progress (Ok For Agent To Fix = yes)

| Title | Sev | Product |
|---|---|---|
| Last active tent Off leaves room appliance demands latched ON | Critical | DSC-Hub |
| ESP-NOW silent recovery infinite reboot when hub absent | Critical | DSC-Control |
| Dashboard root-zone/mat status shows POT1 instead of coldest voted root | High | Dashboard |
| API recovery reboot can interrupt active >35 °C emergency purge | Critical | DSC-Hub |
| Emergency >35 °C purge leaves dehumidifier demand ON | Critical | DSC-Hub |
| Humidifier/Dehumidifier Auto OFF leaves demand latched ON | Critical | DSC-Hub |
| Idle/unplugged panel falsely drives hub WiFi-bounce ladder | Critical | DSC-Hub |
| HA handshake 60s ping vs hub 45/90/180s recovery mismatch | Critical | HA Packages |
| Appliance console Relay toggle can leave Sonoff stuck ON vs demand OFF | High | Dashboard |

These are software-closable. A version that calls itself complete must either fix them with a live prove, or explicitly keep them open as known defects — not silently drop them.

### Won't Fix (leave)

- Sidebar React panel replaces YAML shell but Ops pages are read-only — superseded by Climate command on `/dsc-hub`.
- Catalog Use in Build overwrites nutrient slot 1 — superseded by later Compose work; **do not** treat as closed without a Compose soak.

### Triaged (43)

Themes that a completion plan must not ignore: light-ledger / catch-up dark-period (several Critical), SoftAP adopt/BSSID (park with F-004 — do not deepen ESP-NOW), follower drift / notify entity_ids, Build-a-Plant assign/Want SoT, panel tick/history storms (some may already be Wave 0 — **re-prove**, do not mark Done from chat), HELD-reading pot-switch, Twin links to `/dsc-hub-pro/*`.

---

## 6. Stashes (do not apply blindly)

| Stash | Contents | Verdict |
|---|---|---|
| `stash@{0}` autostash | `services/cannalib/docker-compose.yml` (+5/−4) | **Drop or fold only after CannaLib Recreate.** Hub copy is STALE and has a key. |
| `stash@{1}` temp-before-rebase | Deletes brain move-note, icon svg generator | **Drop** — cleanup already on master. |
| `stash@{2}` wip non-softap junk | `dist/dsc-system-map-card.js` | **Inspect vs current dist drift; do not apply raw.** |
| `stash@{3}` all tracked wip before softap rebase | Version yaml + panel bundle + dash IIFEs | **Fold only as the version-align commit** after diff vs HEAD. Likely the 7.1.x bump that never landed cleanly. |
| `stash@{4}` frontend wip for softap push | `useHeldReading`, `sensorHonesty`, Mission/Grow/Fleet pages | **Fold if HEAD is missing those honesty bits** (Bug Box HELD-across-pot). Diff first. |
| `stash@{5}` wip tracked junk | scrape pids + deleted fat system-map | **Drop junk.** The system-map delete may be the dist-heal — cherry-pick that file only if it matches www. |

---

## 7. What “complete” is allowed to mean

Recommended name: **Hub expected 6.0.0.0 / HA surface 7.2.0**.

Why those numbers:

- **6.0.0.0** is what the live SoftAP fleet already reports (hub, pot1/2/4, heater, heatmat, dehum). Keeping expected **5.2.0** while every node says 6.0.0.0 is a permanent fleet **warn** — a lie. Repo hub/pot *bodies* still say 5.2.0; that is part of the completion pass (align YAML `project.version` + `input_text.dsc_expected_release` to the flashed train, or flash back — do not leave both).
- **7.2.0** is a single surface string to replace 7.1.3 / 7.1.4 / 7.1.7 / 7.1.8-cutover. One value in `dsc_v4_version.yaml`, `const.py`, App chrome, and the panel cache-buster.

Complete **may** mean:

- Software honesty: HELD/OOS/unassigned stay dim/dashed; no 0-for-unavailable; tank alerts gated; Research is CannaLib + cultivar-only; versions agree; `dist/` == `www/` for every card.
- Software-closable Bug Box In Progress items either proven fixed or listed as known defects in RELEASE.
- Planned OOS (AC, mister, POT3, tank) remains gated. Full Auto on a reduced kit names the holes.
- Sync path valid; no secrets in tracked compose.
- Assist stays opt-in.

- Firmware README / headers aligned to live 6.0.0.0 / surface 7.2.0 in-tree.
- [`RELEASE.md`](../RELEASE.md) / [`INSTALL.md`](../INSTALL.md) describe `/dsc-hub`, surface **7.2.0**, expected firmware **6.0.0.0**. Sidebar is not DSC-HUB Pro.

---

## 9. Surface 7.2.0 execution pass (2026-08-17 ~20:00 AEST)

Code in the working tree. **Not** live on HA yet (`HA_TOKEN` unset; no push; Sync add-on has nothing new to pull). Notion Bug Box **not** marked Fixed.

### Repo / compile

| Item | Result |
|---|---|
| HA surface strings | **7.2.0** (`const.py`, packages version helper, panel chrome, `BUNDLE_V`) |
| Expected firmware | **6.0.0.0** |
| Panel rebuild | `npm run build` → `dsc-hub-panel.js` contains **SURFACE 7.2.0** |
| `dist/` vs `www/` | SHA match including `DSC-HUB.js` and `dsc-system-map-card.js` (no longer the same 1 MB blob) |
| Compose secret | `CANNALIB_API_KEY` absent from Hub stale compose |
| Hub compile | **PASS** — `python -m esphome compile dsc-hub.yaml` from `C:\Users\cmgwe\esphome-dsc\v4` (ESPHome 2026.7.4), `Successfully compiled program` |
| Hub OTA | **BLOCKED** — live hub IP `sensor.dsc_hub_ip_address` = **192.168.4.10** (SSID `DSC-Anchor`). Laptop has no route; `192.168.86.33:3232` refused. HA ESPHome Builder lists **DSC-HUB Offline**. Control Offline. **Did not OTA pots.** |
| Control D11 | YAML reboot skip when `gv_hub_last==0` already in tree; **not flashed** (F-007 still unmeasurable) |

### Live HA at soak (still 7.1.x pack)

| Check | Observed |
|---|---|
| Surface chrome | **7.1.4** painted / entity **7.1.3** — until package+panel sync |
| Expected FW | still **5.2.0** → fleet **warn** vs flashed **6.0.0.0** |
| Tank OOS | `dsc_tank_in_service` **off**; tank EC/pH alerts **on**; alert count **2** — B1/B2 not live yet |
| AC / mister / POT3 | in-service **off**; `dsc_reduced_kit` **off** |
| Climate | hub back **on**; Room 22.1 °C / 65 % / 0.94 kPa (not Got 0.0 while live) |
| Twin `/live/twin` | React route; **THREE.js not loaded — redeploy DSC-HUB bundle** (live panel assets stale) |
| Flaps 24h | **357** (was 327 at Phase 1) |
| Assist I-25 | **PASS** — `GetLiveContext` name `dsc` → no exposed entities |
| Lovelace `/dsc-hub-pro/ops` | opened from this host; YAML cards not visible in that navigation (fallback still in tree; do not hide) |

### Still hardware-blocked / parked

AC, clone mister, POT3, tank FW, GPIO `entities.main_light` empty, intake `*_allocated` CFM missing, SCD41 informational, ESP-NOW protocol (0xD5 / 0xD0 / SoftAP BSSID adopt / Nest-STA). F-007 until Control is on HA.

### To make 7.2.0 true on the box

1. Set `HA_TOKEN` (or use DSC-HUB Sync after a push) and sync packages + `custom_components/dsc_hub` + www.
2. Hard-reload HA. Confirm surface **7.2.0**, expected **6.0.0.0**, tank alerts **off** while tank OOS.
3. OTA hub from a host that can reach **192.168.4.10** (HAOS with SoftAP route, or ESPHome Builder once it sees the hub). Quiet-flap window; do not OTA pots.
4. Re-soak I2–I8. Then mark Bug Box Fixed **with that evidence**; leave Fix Approved for the operator.

