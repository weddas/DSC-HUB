# ==================================================================
#  DEPRECATED — moved to packages/dsc_v4_automations.yaml
#  ------------------------------------------------------------------
#  DSC automations now ship as a Home Assistant package so git-push
#  sync can overwrite them without clobbering non-DSC automations in
#  /config/automations.yaml.
#
#  Do not merge this file into HA. Use:
#    homeassistant/packages/dsc_v4_automations.yaml → /config/packages/
#
#  One-time on a live site that already imported these ids: delete the
#  duplicate DSC automations from the UI or /config/automations.yaml
#  after the package is installed (same ids would collide).
# ==================================================================
