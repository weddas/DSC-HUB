# DSC-Heater — HA stub (studio LAN)
# Entity contract: switch.dsc_heater_main_relay
substitutions:
  name: dsc-heater
  friendly_name: DSC-Heater
  ap_ssid: "DSC-Heater Fallback Hotspot"
  wifi_ssid: !secret wifi_ssid
  wifi_password: !secret wifi_password
  lan_ip: "192.168.86.50"
  softap_ip: "192.168.86.50"
  softap_bssid: "00:00:00:00:00:00"
  api_key_val: !secret dsc_heater_api_key
  ota_password_val: !secret dsc_heater_ota_password
  ap_password_val: !secret dsc_heater_ap_password
  api_grace_ms: "60000"

packages:
  common:
    url: https://github.com/weddas/DSC-HUB
    ref: master
    files: [firmware/v4/dsc-sonoff-common.yaml]
    refresh: 1h
