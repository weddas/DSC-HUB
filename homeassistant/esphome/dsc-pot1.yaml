# DSC-POT#1 — HA stub (pulls common package from GitHub)
# Entity contract: sensor.dsc_pot1_soil_*
substitutions:
  name: dsc_pot1
  friendly_name: DSC-POT#1
  wifi_ssid: !secret wifi_ssid
  wifi_password: !secret wifi_password
  espnow_key: !secret espnow_key
  api_key_val: !secret dsc_pot1_api_key
  ota_password_val: !secret dsc_pot1_ota_password
  ap_password_val: !secret dsc_pot1_ap_password

packages:
  common:
    url: https://github.com/weddas/DSC-HUB
    ref: v4.0.0-alpha.1
    files: [firmware/v4/dsc-pot-common.yaml]
    refresh: 1d
