# DSC-Humidifier — HA stub (pulls common package from GitHub)
# Entity contract: switch.dsc_humidifier_main_relay
substitutions:
  name: dsc-humidifier
  friendly_name: DSC-Humidifier
  ap_ssid: "DSC-Humidifier Fallback Hotspot"
  wifi_ssid: !secret wifi_ssid
  wifi_password: !secret wifi_password
  api_key_val: !secret dsc_humidifier_api_key
  ota_password_val: !secret dsc_humidifier_ota_password
  ap_password_val: !secret dsc_humidifier_ap_password
  api_grace_ms: "90000"

packages:
  common:
    url: https://github.com/weddas/DSC-HUB
    ref: v5.0.0
    files: [firmware/v4/dsc-sonoff-common.yaml]
    refresh: 1d
