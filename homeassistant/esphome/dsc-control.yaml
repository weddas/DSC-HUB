# DSC-CONTROL — HA stub (pulls panel body from GitHub; glyphs via sibling include)
# api/ota/wifi live in dsc-control-common.yaml. Since 4.0.9 the panel API is
# plaintext (api_key_val unused) and mDNS is off — add by IP with no key.
# "Unable to connect… api section" ⇒ unreachable host or stale encrypted
# HA entry, not a missing api: block. Recovery: firmware/v4/README.md.
substitutions:
  name: dsc-control
  friendly_name: DSC-CONTROL
  wifi_ssid: !secret wifi_ssid
  wifi_password: !secret wifi_password
  api_key_val: !secret dsc_control_api_key
  ota_password_val: !secret dsc_control_ota_password
  ap_password_val: !secret dsc_control_ap_password
  hub_mac: "84:1F:E8:16:E6:60"
  espnow_cmd_tag: "54727"

packages:
  panel:
    url: https://github.com/weddas/DSC-HUB
    ref: master
    files:
      - firmware/v4/dsc-control-common.yaml
    refresh: 1d
