# DSC-De-Humidifier — HA stub (pulls common package from GitHub)
# Entity contract: switch.dsc_de_humidifier_main_relay
substitutions:
  name: dsc-de-humidifier
  friendly_name: DSC-De-Humidifier
  ap_ssid: "DSC-De-Humidifi Fallback Hotspot"
  wifi_ssid: !secret wifi_ssid
  wifi_password: !secret wifi_password
  anchor_ssid: "DSC-Anchor"
  anchor_ap_password: !secret dsc_anchor_ap_password
  softap_ip: "192.168.4.23"
  api_key_val: !secret dsc_dehumidifier_api_key
  ota_password_val: !secret dsc_dehumidifier_ota_password
  ap_password_val: !secret dsc_dehumidifier_ap_password
  api_grace_ms: "240000"

packages:
  common:
    url: https://github.com/weddas/DSC-HUB
    ref: master
    files: [firmware/v4/dsc-sonoff-common.yaml]
    refresh: 1h
