# DSC-POT#3 — HA stub
# SoftAP fleet home 192.168.4.14 (via bridge eth 192.168.86.66).
substitutions:
  name: dsc_pot3
  friendly_name: DSC-POT#3
  wifi_ssid: !secret wifi_ssid
  wifi_password: !secret wifi_password
  anchor_ssid: "DSC-Anchor"
  anchor_ap_password: !secret dsc_anchor_ap_password
  softap_bssid: "58:2A:BD:60:3C:1D"
  softap_ip: "192.168.4.14"
  wifi_bssid: "58:2A:BD:60:3C:1D"
  espnow_key: !secret espnow_key
  api_key_val: !secret dsc_pot3_api_key
  ota_password_val: !secret dsc_pot3_ota_password
  ap_password_val: !secret dsc_pot3_ap_password
  hub_mac: "84:1F:E8:16:E6:60"
  fleet_pot_index: "3"

packages:
  common:
    url: https://github.com/weddas/DSC-HUB
    ref: master
    files:
      - firmware/v4/dsc-pot-wifi-lab.yaml
      - firmware/v4/dsc-pot-common.yaml
      # No dsc-fleet-setup-pot-lab.yaml — lab peers use ${hub_mac}.
    refresh: 1h

wifi:
  use_address: 192.168.4.14
